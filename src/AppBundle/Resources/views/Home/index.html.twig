{% extends "::base.html.twig" %}

{% block title %}Home page - {{ parent() }}{% endblock %}


{% block body %}

    <header id="starting-page">

        {# not needed for now #}
        {#<nav>#}
            {#<ul>#}
                {#<li><a href="#">Nav 1</a></li>#}
                {#<li><a href="#">Nav 2</a></li>#}
                {#<li><a href="#">Nav 3</a></li>#}
                {#<li><a href="#">Nav 4</a></li>#}
            {#</ul>#}
        {#</nav>#}

        <div id="text">
            <h1>Keep Running</h1>
            <p>
                Keep Running skirtas žmonėms, kuriems nusibodo įprasta bėgimo trasa ir nori kažko naujo. Su šiuo įrankiu galėsite patogiai susigeneruoti visiškai atsitiktinę bėgimo trasą.
            </p>
            <a href="#filters">Pradėti</a>
        </div>
    </header>

    <div id="main-page-sections">
        <section id="filters">
            <div class="container">
                <h1>Filtrai</h1>
                <form action="{{ path('map.index') }}">
                    <div class="form-group">
                        Trasos ilgis:
                        <span><input id="distance" type="text" readonly=""> km</span>
                        <input id="distance_from" type="hidden"  name="distance_from">
                        <input id="distance_to" type="hidden"  name="distance_to">
                        <div id="distance_slider_range"></div>
                    </div>
                    <div class="form-group">
                        Sunkumas:
                        {% for level in tracks_levels %}
                            <input type="radio" name="difficulty" value="{{ level.levelId }}"> {{ level.levelName }}
                        {% endfor %}
                    </div>
                    <div class="form-group">
                        <button>Generuoti trasą</button>
                    </div>
                </form>
            </div>
        </section>

        <section id="about-project" style="display: none;">
            <div class="container">
                <h1>Apie projektą</h1>
                <p>
                    Lorem ipsum dolor sit amet, consectetur adipisicing elit. Ad aut iste itaque minus modi mollitia quo reiciendis! Architecto delectus est hic ipsa iusto totam ullam vitae? Ad adipisci at distinctio laudantium nulla optio quo sequi sit tenetur vel. Aliquam architecto commodi cumque ducimus, possimus quod veritatis. Cumque debitis ipsam voluptas!
                </p>
            </div>
        </section>
    </div>

    <footer>
        <div class="container">
            <p>
                NFQ Akademija 2016, Kaunas.
            </p>
        </div>
    </footer>

{% endblock %}
{% block javascripts %}
    <script src="//code.jquery.com/jquery-1.10.2.js"></script>
    <script src="//code.jquery.com/ui/1.11.4/jquery-ui.js"></script>
    <script type="text/javascript">
        $(function () {
            $("#distance_slider_range").slider({
                range: true,
                values: [2, {{ max_distance }}],
                min: 2,
                max: {{ max_distance }},
                slide: function( event, ui ) {
                    $( "#distance" ).val( ui.values[0] + " - " + ui.values[1] );
                    $( "#distance_from" ).val(ui.values[0]);
                    $( "#distance_to" ).val(ui.values[1]);
                }
            });
            $( "#distance" ).val( $( "#distance_slider_range" ).slider( "values", 0 ) +
                    " - " + $( "#distance_slider_range" ).slider( "values", 1 ) );
        });
    </script>
{% endblock %}